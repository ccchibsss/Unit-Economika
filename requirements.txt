# ============================================================================
# 🔴 ОБЯЗАТЕЛЬНЫЕ (Core) — без них приложение не запустится
# ============================================================================
streamlit>=1.38.0
pandas>=2.2.0
numpy>=1.26.0
requests>=2.31.0
python-dateutil>=2.8.2
pytz>=2024.1
chardet>=5.2.0
psutil>=5.9.0

# ============================================================================
# 🟡 РЕКОМЕНДУЕМЫЕ (Full Functionality) — Excel, графики, ML, БД
# ============================================================================
# --- Excel / Отчёты ---
openpyxl>=3.1.2
xlsxwriter>=3.2.0
python-calamine>=0.2.0          # Быстрый движок для read_excel
reportlab>=3.6.0                # PDF-экспорт

# --- Базы данных и быстрые DataFrame ---
polars>=1.0.0
duckdb>=1.0.0
pyarrow>=16.0.0

# --- Графики и визуализация ---
plotly>=5.22.0
matplotlib>=3.8.0
seaborn>=0.13.0

# --- ML и классификация ---
scikit-learn>=1.5.0
joblib>=1.4.0

# --- Утилиты ---
Babel>=2.14.0                   # Локализация чисел/валют
tabulate>=0.9.0                 # Красивые таблицы
validators>=0.28.0              # Валидация email/url
pycountry>=22.3.5               # Коды стран
holidays>=0.47                  # Производственный календарь
phonenumbers>=8.13.0            # Парсинг телефонов
tzlocal>=5.2                    # Локальная таймзона

# ============================================================================
# 🟢 ОПЦИОНАЛЬНЫЕ (Advanced) — AI, асинхронность, альтернативные графики
# ============================================================================
# --- AI / LLM ---
openai>=1.30.0
anthropic>=0.25.0
tiktoken>=0.7.0                 # Токенизация для LLM

# --- Асинхронный HTTP ---
aiohttp>=3.9.0
aiofiles>=23.2.0
httpx>=0.27.0
websockets>=12.0

# --- Альтернативные графики ---
altair>=5.3.0
bokeh>=3.4.0

# --- Валидация данных ---
pandera>=0.19.0

# --- Распределённые вычисления (тяжёлые, ставить только при необходимости) ---
# ray>=2.10.0                   # Раскомментировать если нужна распределёнка
# modin[all]>=0.28.0            # Раскомментировать для параллельного pandas

# --- Прочее ---
Pillow>=10.3.0                  # Обработка изображений
retry>=0.9.2                    # Декоратор retry (используется в Блоке 11)
